name: QA Checks

on:
  push:
  workflow_dispatch:   # lets you also trigger it manually to test

jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Broken-link and HTML/CSS validation
        run: python3 scripts/qa-checks.py
